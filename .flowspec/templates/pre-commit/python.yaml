# Pre-commit hooks configuration for Python projects
# https://pre-commit.com/
#
# Install: pip install pre-commit && pre-commit install
# Run manually: pre-commit run --all-files
# Update hooks: pre-commit autoupdate
#
# Generated by flowspec init for Python projects

repos:
  # =============================================================================
  # Universal hooks
  # =============================================================================
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
        name: Trim trailing whitespace
      - id: end-of-file-fixer
        name: Fix end of files
      - id: check-yaml
        name: Check YAML syntax
        args: [--unsafe]
      - id: check-json
        name: Check JSON syntax
      - id: check-toml
        name: Check TOML syntax
      - id: check-merge-conflict
        name: Check for merge conflict markers
      - id: check-added-large-files
        name: Check for large files
        args: [--maxkb=500]
      - id: detect-private-key
        name: Detect private keys
      - id: check-ast
        name: Check Python AST
      - id: debug-statements
        name: Check for debug statements

  # =============================================================================
  # Python-specific hooks
  # =============================================================================
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.3
    hooks:
      - id: ruff
        name: Ruff linter
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format
        name: Ruff formatter

  - repo: https://github.com/PyCQA/bandit
    rev: 1.7.10
    hooks:
      - id: bandit
        name: Security check with bandit
        args: [-r, --skip, B101, -x, tests]  # Skip assert warnings and tests

  # =============================================================================
  # Optional Python hooks (uncomment as needed)
  # =============================================================================
  # Type checking with mypy:
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.13.0
  #   hooks:
  #     - id: mypy
  #       name: Type check with mypy
  #       additional_dependencies: [types-all]
  #       args: [--ignore-missing-imports]
  #
  # Import sorting (if not using ruff):
  # - repo: https://github.com/pycqa/isort
  #   rev: 5.13.2
  #   hooks:
  #     - id: isort
  #       name: Sort imports
  #
  # Docstring coverage:
  # - repo: https://github.com/econchick/interrogate
  #   rev: 1.7.0
  #   hooks:
  #     - id: interrogate
  #       args: [-vv, --fail-under=80]

  # =============================================================================
  # Flowspec security scanning (recommended)
  # =============================================================================
  # Uncomment for fast security scanning before commits:
  # - repo: local
  #   hooks:
  #     - id: flowspec-security-fast
  #       name: Flowspec Security Scan (fast)
  #       entry: flowspec security scan --fast --changed-only --fail-on critical
  #       language: system
  #       pass_filenames: false
  #       stages: [pre-commit]
